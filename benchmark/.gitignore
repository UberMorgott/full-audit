# Generated by score.py — not committed.
score-result.json
fixtures/score-result.json

# The repo root .gitignore ignores all `audit-bugs.json` (live audit artifact).
# The fixture below is an intentional, committed test input — re-include it.
!fixtures/audit-bugs.json
